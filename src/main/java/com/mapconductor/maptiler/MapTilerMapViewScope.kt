package com.mapconductor.maptiler

import com.mapconductor.compose.MapViewScope

/**
 * MapTiler 固有のオーバーレイ用スコープ拡張ポイント。
 * 現状は共通の [MapViewScope] をそのまま利用する。
 */
class MapTilerMapViewScope : MapViewScope()
